# AGENTS.md

<!--
本文件是常驻层：每轮对话都在上下文里，所以每一行都在收费。
新增内容前先问一句：这条规则是不是每个任务都可能撞上？
不是 → 写进 docs/，这里只留一行指针。
-->

## 项目

点开一个文件夹，立刻以知识库方式浏览里面的 Markdown：左侧目录树，右侧渲染结果。替代“为每个文件夹建一个 Obsidian vault”这个动作，目标是零配置、低资源占用。

当前状态：需求已对齐，见 `docs/spec.md`。第一版正在实现。

- 技术栈：Node + TypeScript
- 包结构：
  - `src/` — 服务与渲染
  - `public/` — 浏览器端页面与样式
- 入口：`md-viewer.cmd`（命令行或拖拽传入库根；无参数则弹选择框）

## 项目配置

一次性填好，之后手改。冲突时以本节为准，覆盖任何工具的默认值。

- 问题追踪：无
- triage 标签：不适用（无追踪器）
- 文档根：`docs/`

## 硬禁令

没有例外，不解释。

- 禁止假设超时等于需要重启
- 禁止给测试加认证检查、环境变量门控或条件跳过
- 禁止顺手清理；只做你本来要做的改动
- 禁止防御性检查，禁止"以防万一"的 try/catch
- 禁止把不确定写成注释；不确定就去查
- 禁止留注释掉的代码，禁止 `TODO: implement` 空壳
- 禁止在一个提交里混入与主题无关的改动
- 未经明确授权，禁止推送、发布、部署、删数据
- 禁止用摘要代替证据；证据是别人能亲自查看的原始输出

## 判断规则

每条写的是征兆，不是道理。看到征兆就停下重看。

- 想 mock → 生产模块缺端口。去修注入缝，不要 mock 绕过
- 一个函数只被调用一次 → 那是间接，不是抽象
- 想建 `-utils` / `-helpers` / `-manager` / `-handler` 后缀的文件 → 路径没承载领域，先想清楚它是什么
- 同一个判别器在 3 个以上文件里分支 → 收成策略表
- 想新建一个协调器来包住现有协调器 → 问题被重塑了，停下重看
- 删掉这个模块后调用方直接到达他们想要的东西 → 删掉它
- 状态里同时出现 isLoading / error / data → 换成判别联合
- 越过校验边界后还在写 `?.` / `??` → 边界没做干净，回去修边界
- 一个测试在删掉系统某部分后仍然通过 → 它在测结构，不在测行为
- 测试偶发失败 → 找方差来源修掉它，不许删测试
- 模块级带 `/g` 的正则被递归调用 → 每次新建实例。共享的 `lastIndex` 会被递归里层重置，外层循环转不出去（实测：挂死 24 秒后 OOM）

## 工作信号

命中哪条就做哪件事。这张表是行为指令，不是建议。

- 需求说不清、反复改口 → 先做对齐提问，把问题问完再动手（有 grilling 类技能就用它）
- 要动 3 个以上模块或新增模块 → 先写一份 spec 到 `docs/`，再动手实现
- 第 1 次出现回归或跨模块缺陷 → 查根因，然后启用 `docs/qa.md`
- 要写第 2 个测试文件 → 启用 `docs/testing.md`
- 出现第 2 个同类模块 → 启用 `docs/coding-standards.md`
- 同一决策被问第 2 次 → 在 `docs/rejected/` 建记录，术语补进 `docs/glossary.md`
- 文档累积到 3 篇 → 维护 `docs/index.md`
- 宣布完成之前 → 过下面的证据门槛四问

## 证据门槛

说"做完了"之前，四个问题都要有答案：

1. 它工作得够好吗？
2. 它回归了别的东西吗？
3. 受影响的每一处都测过了吗？
4. 覆盖是真的吗？

答不上来的那条，就是还没做完。

## 文档

- `docs/index.md` — 文档索引。新增文档必须同步这里
- `docs/glossary.md` — 术语表。一个概念只有一个词
- `docs/_inbox.md` — 未启用清单，每条带启用条件
- `docs/rejected/` — 已否决记录，一件一文件

冲突仲裁：本文件 > 继承来的规则 > 任何工具或 skill 的默认值。
